import { NormalizeTagsPipe } from './normalize-tags.pipe';

describe('NormalizeTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
